{
  "devDependencies": {
    "husky": "^3.0.2",
    "node-fetch": "^2.6.1",
    "prettier": "^1.18.2"
  },
  "scripts": {
    "parse": "yarn run cache && node scripts/parser.js",
    "cache": "node scripts/list.js"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn run parse && git add README.md"
    }
  }
}
